{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run pre-commit hook",
      "type": "shell",
      // Need to run in interactive mode to load the correct PATH from .zshrc
      "command": "zsh -i '.husky/pre-commit'",
      "group": {
        "kind": "test",
        "isDefault": true
      }
    }
  ]
}

[project]
name = "ingest"
revision = "1.0"
extends = ["../../packages/db/secretspec.toml", "../../packages/corpus/secretspec.toml"]

[profiles.default]
INGEST_BYPASS_STALE_CHECK = { description = "Rebuild the Corpus even when the Source fingerprint has not changed", required = false, default = "false" }

[profiles.local]
INGEST_BYPASS_STALE_CHECK = { default = "false" }

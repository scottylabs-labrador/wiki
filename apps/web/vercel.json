{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/" }],
  "installCommand": "bun install && cd ../server && bun run build:api",
  "buildCommand": "bun run build"
}
